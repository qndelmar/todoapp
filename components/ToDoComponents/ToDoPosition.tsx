import {FC} from "react";
import {ITodo} from "@/interfaces/ITodo";

const ToDoPosition:FC<ITodo> = ({name, color, isClosed, importance}) => {
    return (
        <div>

        </div>
    );
};

export default ToDoPosition;